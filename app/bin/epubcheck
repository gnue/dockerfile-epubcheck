#!/bin/bash

EPUBCHECK=${EPUBCHECK:-epubcheck-4.0.1}

epubcheck() {
    echo "==> $@"
    java -jar "/app/$EPUBCHECK/epubcheck.jar" "$@"
}

if [ -n "$*" ]; then
    epubcheck "$@"
else
    for file in data/*.epub; do
       epubcheck "$file"
    done
fi
