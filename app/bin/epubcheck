#!/bin/bash

epubcheck() {
    echo "==> $@"
    java -jar "/app/epubcheck/epubcheck.jar" "$@"
}

if [ -n "$*" ]; then
    epubcheck "$@"
else
    for file in data/*.epub; do
       epubcheck "$file"
    done
fi
